## Development Commands

- Use `bun install` instead of `npm install`
- Use `bun run test` to run tests

## Agent Guidelines

- Always prefer simplicity over pathological correctness. YAGNI, KISS, DRY.
- No backward-shims or fallback paths unless they come free without adding cyclomatic complexity
